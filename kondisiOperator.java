public class kondisiOperator {
    public static void main(String[] args) {
        int score = 0;
        char answer = 'a';
        
        score = (answer == 'a')? 10 : 0;
        /*checking the condition of 'answer' variable
        if answer defined as 'a', then the score value is 10
        else, the score is 0.*/
        System.out.println("Score = " + score);
}
}
